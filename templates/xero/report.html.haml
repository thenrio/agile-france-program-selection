%html
  %p= "#{problems.size} problem(s) occured"
  %ul
    - problems.each do |problem|
      %li= problem.backtrace
  %p= "Amongst #{invoices.size} posted invoices, the following require our attention"
  %ul
    - invoices.each do |invoice|
      - if invoice.has_conflicting_emails?
        %li= "#{invoice.inspect} should be sent to #{invoice.email}"